1. What is Kubernetes? (Simple Definition)

Kubernetes is a container orchestration tool.

It:

runs Docker containers

manages them automatically

keeps applications alive, scalable, and stable

💡 Docker runs containers.
💡 Kubernetes runs Docker at scale.

2. The Core Problem After Docker

Docker solves:

“It runs the same everywhere”

But Docker alone cannot handle:

❌ Hundreds of containers
❌ Multiple servers
❌ Auto-restart on failure
❌ Load balancing
❌ Scaling up/down
❌ Zero-downtime deployments


3. Real Problem Scenario (Before Kubernetes)

Imagine:

You have 50 containers

Running on 10 servers

One server crashes 💥

Questions you must answer manually:

Which containers died?

Where should they restart?

How to balance traffic?

How to deploy a new version safely?

➡️ This is operational hell.


4. What Kubernetes Solves (The Solution)

Kubernetes automates container management.

✅ Kubernetes provides:

Auto-scheduling

Auto-restarts

Auto-scaling

Load balancing

Self-healing

Rolling updates

Service discovery

📌 You tell Kubernetes WHAT you want, not HOW to do it.

5. Core Kubernetes Concepts (Very Important)

Concept	Meaning
Cluster	Group of machines
Node	  One machine (VM or physical)
Pod	Smallest   unit (1+ containers)
Deployment	  Manages replicas
Service	Exposes pods to network
ConfigMap	  App configuration
Secret	     Sensitive data
Ingress	External HTTP access

8. Kubernetes Architecture (High-Level)
Control Plane (Brain)

API Server

Scheduler

Controller Manager

etcd (state storage)

Worker Nodes (Muscle)

kubelet

container runtime (Docker / containerd)

pods

9. Docker vs Kubernetes (Clear Difference)
Docker	Kubernetes
Runs containers	Manages containers
Single machine focus	Multi-machine clusters
Manual scaling	Auto scaling
No self-healing	Self-healing
No traffic routing	Built-in load balancing



kubernetes
:
container orchestration
master of container

k8s
deploy - get a cluster ( group of nodes )
master | worker nodes

components
api serve

POD
single instance of

master
api server ()
schedular
ETCD
control manager

worker node
kubelet


features

installation


Deployment
Pod - container - image


kubectl create deployment my-ngnix(name) -- image =nginx(repo):latest

kubectl get deployments
kubectl get pods

minikube dashboard
port binding
service object (image - server(outside))
kubectl expose deloyment my-ngnix(name) --port = 80 --type=LoadBalancer

minikube service my-nginx


commands used

minikube start | delete (cluster)
minikube status

minikube dashboard

kubectl create deployment my-ngnix(name) -- image =nginx(repo):latest

kubectl get deployments
kubectl get pods


kubectl delete deployment my-ngnix(name)


Basic modules
project

Build docker image

push public repo


minikube start

kubectl create deployment my-ngnix(name) -- image =nginx(repo):latest


kubectl get deployments (status)

kubectl get pods

kubectl logs (pods name)

kubectl describe pods (extra info)


k8 rollout
update - changes

new Image - push

kubectl set image deployment my-ngnix(name) -- weappdemo(container name) (new image name)


reverse the image

kubectl set image deployment my-ngnix(name) -- weappdemo(container name) (new image name) - incorrect path - updating stuc


kubectl rollout status deployment mywebapp(image name)

kubectl rollout undo deployment mywebapp(image name)


self healing

auto restart - uprunning

scaling
stop not for sec - no down time
multiple instance

kubectl scale deloyment node-app-replicas=4

# yaml config

(deployment | pods )

deployment
anyname.yml

kubectl apply -f anyname.yml



service
anyname.yml

kubectl get services

kubectl apply -f anyname.yml
minikube service my-nginx(service name)



multiple container
app - db

local
docker run -p 27017:27017 -d --name mongodb(name) mango

npm install

pull image

docker pull imagename

docker run -d -p 27017:27017 --network my-net --name mango mango

docker run --network my-net -p 3000:3000 --name myapp (pull repo)

deploy
multi container - pod
run each container in seperate pod

single POD
two image - node - mongo

deploy - containers - multiple container
service -

single file

 deploy
 ---
 service



difference POD

dynamic port and lost change
using env

mongo config



volumes & data
Data safe

outside cluster

persistant volumn

host-pv.yml

kubectl get sc (store class) (handle data)











upto : 1:35
